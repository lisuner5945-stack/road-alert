package ru.example.roadalert.ui

/** Экраны приложения. Навигация простая: стек в MainViewModel, без лишних библиотек. */
enum class Screen {
    ONBOARDING,
    HOME,
    DRIVE,
    HUD,
    SETTINGS,
    ABOUT,
    DEVELOPER,
}
